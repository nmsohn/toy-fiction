version: "3.9"

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: narrative-engine-db
    environment:
      POSTGRES_DB: narrative_engine
      POSTGRES_USER: narrative_user
      POSTGRES_PASSWORD: narrative_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U narrative_user -d narrative_engine"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
